# expect: 'requires' is a required property
# INVALID: `requires` is required, as `[]` when the manifest declares no
# dependencies. Without it, orphan cleanup and the check that a removal leaves
# nothing unsatisfied cannot be computed offline — answering "does anything still
# need this?" would mean refetching every installed package's manifest.
edgetx_format_version: "1.0"
packages:
  - id: github.com/acme/simple-tool
    version: "1.0.0"
    reason: explicit
    source:
      repo: github.com/acme/simple-tool
      channel: tag
      ref: "v1.0.0"
      commit: "3f9a1c0e4b7d2a5f8c1e6b0d9a4f7c2e5b8d1a03"
      manifest_path: "edgetx.yml"
