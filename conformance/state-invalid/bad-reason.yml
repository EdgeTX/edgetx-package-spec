# expect: 'auto' is not one of
# INVALID: `reason` must be exactly `explicit` or `dependency`. Orphan cleanup
# depends on the distinction, so a third value has no defined behaviour.
edgetx_format_version: "1.0"
packages:
  - id: github.com/acme/simple-tool
    version: "1.0.0"
    reason: auto
    requires: []
    source:
      repo: github.com/acme/simple-tool
      channel: tag
      ref: "v1.0.0"
      commit: "3f9a1c0e4b7d2a5f8c1e6b0d9a4f7c2e5b8d1a03"
      manifest_path: "edgetx.yml"
