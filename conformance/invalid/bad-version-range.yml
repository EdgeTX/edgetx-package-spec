# INVALID: min_edgetx_version uses an invalid format (not semver / wildcard-patch).
# min_edgetx_version must match MAJOR.MINOR.PATCH or MAJOR.MINOR.x
package:
  id: github.com/acme/bad-version
  description: Package with malformed version strings
  min_edgetx_version: "2.x"
  max_edgetx_version: "3.0.0"
