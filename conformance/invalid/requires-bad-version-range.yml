# expect: '2\.x' does not match
# INVALID: `requires[].version` is not a supported range form.
edgetx_format_version: "1.0"
package:
  id: github.com/acme/bad-range
  description: Package with an unsupported dependency version range
requires:
  - id: github.com/someone/elrs-libs
    version: "2.x"
