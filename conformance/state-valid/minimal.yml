# The minimum a conforming package-state file must write: id, version, reason,
# source and requires. Every other field is optional, and an absent field is
# equivalent to an explicit null.
#
# `requires: []` is not decoration — without it, "does anything still need this
# package?" cannot be answered without refetching every installed manifest.
id: github.com/acme/simple-tool
version: null
reason: explicit
requires: []
source:
  repo: github.com/acme/simple-tool
  channel: branch
  ref: main
  commit: "3f9a1c0e4b7d2a5f8c1e6b0d9a4f7c2e5b8d1a03"
  manifest_path: "edgetx.yml"
